<Page
    x:Class="GeoAR.ARRegion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GeoAR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <CaptureElement x:Name="PreviewScreen" Margin="0,0,0,0" />
        <CheckBox x:Name="checkTest" IsChecked="{Binding IsTilted, Mode=TwoWay}" Content="AR" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 0 0 50" />
        <Canvas Name="ItemCanvas"/>

    </Grid>
</Page>
